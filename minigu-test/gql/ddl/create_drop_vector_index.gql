call create_test_vector_graph_data("vec_graph", 20);

session set graph vec_graph;

CREATE VECTOR INDEX vec_idx
FOR (n:PERSON)
ON (n.embedding);

-- negative case: duplicate create should error
CREATE VECTOR INDEX vec_idx
FOR (n:PERSON)
ON (n.embedding);

CREATE VECTOR INDEX vec_idx IF NOT EXISTS
FOR (n:PERSON)
ON (n.embedding);

DROP VECTOR INDEX vec_idx;

DROP VECTOR INDEX vec_idx IF EXISTS;

-- negative case: drop after removal should error
DROP VECTOR INDEX vec_idx;
