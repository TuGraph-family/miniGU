# FinBench TSR1 - Simple Read 1
# Test case: MATCH (n:Account{id:12}) RETURN n.createTime, n.isBlocked, n.type

# This is a query that should return results
query TTT
MATCH (n:Account{id:12})
RETURN
    n.createTime as createTime,
    n.isBlocked as isBlocked,
    n.type as type
---- 